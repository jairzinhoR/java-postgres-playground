class Solution:
    def romanToInt(self, s: str) -> int:
        tamanho = len(s)
        listaLetras = []

        for i in range(tamanho-1):
        listaLetras[i] = s[i]
    
    print(listaLetras)


roman = romanToInt(XXX)

